


cmake ../llvm \
-G Ninja \
-DCMAKE_BUILD_TYPE=Release \
-DLLVM_ENABLE_PROJECTS="clang;lld;..." \
-DLIBCXXABI_USE_COMPILER_RT=ON \
-DLIBCXXABI_USE_LLVM_UNWIND_TABLES=ON









# برگشت به یک دایرکتوری بالاتر (llvm-project)
cd ..

# حذف پوشه build (که حاوی تنظیمات و فایل‌های ساخت خراب است)
rm -rf build

# ایجاد و ورود مجدد به پوشه build
mkdir build
cd build












..........
sudo swapoff /swapfile


sudo rm /swapfile

sudo rm /swapfile


sudo fallocate -l 16G /swapfile  # ایجاد فایل ۱۶G
sudo chmod 600 /swapfile          # تنظیم دسترسی‌ها
sudo mkswap /swapfile             # فرمت کردن به عنوان Swap
sudo swapon /swapfile             # فعال‌سازی Swap
















sudo fallocate -l 16G /swapfile


sudo chmod 600 /swapfile
sudo mkswap /swapfile

sudo swapon /swapfile

swapon --show




