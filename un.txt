# فایل: cnew/compiler/CMakeLists.txt
file(GLOB CNG_SOURCES "*.cpp")

# تنظیمات شامل هدرهای عمومی برای دسترسی به تعاریف توکن‌ها
target_include_directories(CNG_COMPILER PUBLIC .)

# ایجاد یک کتابخانه استاتیک به نام CNG_COMPILER 
add_library(CNG_COMPILER STATIC ${CNG_SOURCES})

# اضافه کردن دایرکتوری هدرهای ما به لیست جستجوی هدرهای عمومی
target_include_directories(CNG_COMPILER PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
